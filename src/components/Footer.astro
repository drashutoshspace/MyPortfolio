---
import { profile } from "~/data/profile";
import Email from "~/components/Email.astro";
import Icon from "~/components/Icon.astro";

const reviewed = new Date().toLocaleDateString("en-GB", { month: "long", year: "numeric" });
---

<footer class="foot">
  <div class="shell inner">
    <div class="col">
      <p class="who">
        <strong>{profile.name}</strong><br />
        {profile.role}, {profile.lab}<br />
        {profile.institutionShort}, {profile.city}, {profile.country}
      </p>
      <p class="mail"><Email /></p>
    </div>

    <nav class="col no-print" aria-label="Profiles elsewhere">
      <p class="key">Elsewhere</p>
      <ul>
        <li><a class="ico" href={profile.links.scholar} rel="me noopener"><Icon name="scholar" size={15} />Google Scholar</a></li>
        <li><a class="ico" href={profile.links.orcid} rel="me noopener"><Icon name="orcid" size={15} />ORCID</a></li>
        <li><a class="ico" href={profile.links.linkedin} rel="me noopener"><Icon name="linkedin" size={15} />LinkedIn</a></li>
        <li><a class="ico" href={profile.links.github} rel="me noopener"><Icon name="github" size={15} />GitHub</a></li>
      </ul>
    </nav>

    <div class="col no-print">
      <p class="key">This site</p>
      <ul>
        <li><a class="link" href="/research/">Research and publications</a></li>
        <li><a class="link" href="/projects/">Projects</a></li>
        <li><a class="link" href="/collaborations/">Collaborations</a></li>
        <li><a class="link" href="/news/">News</a></li>
        <li><a class="link" href="/cv/">Curriculum vitae</a></li>
      </ul>
      <p class="key stamp">Last reviewed {reviewed}</p>
    </div>
  </div>
</footer>

<style>
  .foot {
    border-top: 1px solid var(--rule);
    margin-top: clamp(2.5rem, 6vw, 4rem);
    padding-block: clamp(2rem, 4vw, 2.8rem) 2.6rem;
    background: var(--bg-alt);
  }
  .inner {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    align-items: start;
  }
  .col { display: flex; flex-direction: column; gap: 0.6rem; }
  .who { font-size: 0.9rem; line-height: 1.55; margin: 0; color: var(--ink-2); }
  .mail { margin: 0; font-family: var(--mono); font-size: 0.8rem; }
  ul { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 0.3rem; }
  li { font-size: 0.9rem; }
  .ico {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .ico:hover { color: var(--accent-hover); }
  .ico svg { flex: none; opacity: 0.85; }
  .stamp { margin-top: 0.5rem; }
</style>
